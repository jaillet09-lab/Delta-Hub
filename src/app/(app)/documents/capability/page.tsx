export const dynamic = 'force-dynamic'

import { PrintCapability } from '@/components/documents/PrintCapability'

// Standing capability statement — the same leave-behind that travels with a
// proposal, viewable on its own so the owner can check it any time.
export default function CapabilityStatementPage() {
  return <PrintCapability />
}
